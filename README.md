- 架构与数据流图 —— csv → 入库 → Chroma → 检索 → SSE 的完整链路
- 项目结构表 —— 每个文件的职责
- 快速开始 —— 建虚拟环境 → 配 .env (含完整模板)→ 离线入库 → 启动服务
- 使用说明 —— 页面操作、📎 上传规则、curl 调用示例、SSE 事件格式
- 配置项说明 —— TOP_K / SCORE_THRESHOLD 等参数含义与调优方向
- 注意事项 —— “先入库后启动、重跑脚本要重启服务” 等踩坑要点
